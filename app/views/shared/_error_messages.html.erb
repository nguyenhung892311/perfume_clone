<% if object.errors.any? %>
  <div class="alert alert-danger">
    <h4 class="my-2"><%= "Có #{object.errors.count} lỗi:" %></h4>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li>
          <%= message %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
